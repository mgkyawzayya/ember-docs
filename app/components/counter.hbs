<p>{{this.count}}</p>
<p>x {{@multiple}}</p>
<p>= {{this.total}}</p>

<button type="button" {{on "click" (fn this.change 1)}}>+</button>
<button type="button" {{on "click" (fn this.change -1)}}>-</button>

<button type="button" {{on "click" this.double}}>Double It</button>